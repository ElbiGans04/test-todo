# How To Start ?
### Instal Package First
#### 1. Run Command from command line :
- Yarn
  - `yarn install`
- Npm
  - `npm install`

#### 2. Run Command from command line :
- Yarn
  - `yarn prepare`
- Npm
  - `npm run prepare`
